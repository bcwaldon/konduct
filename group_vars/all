ansible_python_interpreter: PATH=/home/core/bin:$PATH python

control_plane_endpoint: "https://api.{{ aws_hosted_zone }}"

ca_name: kubernetes

kubelet_enable_pod_proxy: true

kubernetes_version: 1.3.9

# Modify the following only if you are running a custom DataDog agent and
# wish to forward telemetry to an external statsd endpoint.
datadog_agent_image: datadog/docker-dd-agent:kubernetes
datadog_statsd_host: 127.0.0.1
datadog_statsd_port: 8125
